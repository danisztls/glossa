# Glossa Catholica — development plan

A living document: what is left, why it matters, what it depends on, and honest sizing — organized so that someone can answer "what should I work on next, and in what order." Distinct from `docs/decisions.md`, which is an **append-only historical log** of decisions as they were made (read it for _why_ something is the way it is, and when it changed).

This document is the opposite shape: it gets rewritten as reality changes and has no history of its own. **It does not restate what shipped.** A phase that lands leaves this file entirely — its "why" belongs in a new `decisions.md` entry, not in a "recently done" section here that would grow without bound and duplicate the log.

Sizing is stated honestly throughout: a measured figure is presented as one (with how it was measured); anything else is explicitly labelled an estimate. Where sizing isn't yet knowable — an item nobody has scoped in detail — that is stated too, rather than guessed at.

## Known gaps

Each stated as: what it is, why it matters, what it depends on, and sizing.

| #   | Gap                                                             | Why it matters                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Depends on                                                                                                                                                                                                                                                                                              | Sizing                                                                                                                             |
| --- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Latin for the CCC and the Magisterium documents**             | Latin is the **normative** text for conciliar and papal documents rather than a third translation, and its coverage fills exactly the gap Portuguese leaves worst (Leo XIII: 64% LA vs. 17% PT). Today the corpus is 239 `en` works, 111 `pt`, and **2 `la`** — the Bible and the Summa. See `docs/research/latin-sources.md` §3–5                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Nothing structural. The site's content-language layer already handles Latin. What is unresolved is a pipeline design question, scoped below                                                                                                                                                             | Not sized. The scrape is a third `lang` argument through an existing parser; the symmetry-check question is the real unknown       |
| 2   | **Full-text search** (prebuilt client-side index)               | Promised in `docs/decisions.md`'s original v1 scope ("Lookup mode"). Nothing exists: `JumpBox` resolves _addresses_, and `site/scripts/` holds a cross-reference builder, a corpus sync and a deploy preflight — no index builder                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Corpus size. 339 document works plus four Bibles, the CCC, the Compendium and the Summa mean "prebuilt" needs real index-size engineering, not a toy                                                                                                                                                    | Not sized — no prototype exists; index format, build-time-vs-runtime tradeoffs, and per-work vs. corpus-wide indexing are all open |
| 3   | **Footnotes as sidenotes (desktop) / tap-popovers (mobile)**    | **This one now gates published corpus data.** The Douay-Rheims carries 1,917 of Challoner's notes, 1,307 chapter arguments and 151 in-chapter lines and headings, all on disk and **none of it rendered** — `docs/decisions.md` (2026-08-24) names this row as the stated prerequisite for showing a gloss at all. The schema fields exist (`notes`, `summary`, `lemma`, `text_marked`); the reader has only the inline expando (`CitationDisclosure.svelte`), which pushes the page down and is the mechanism the designed experience _replaces_ rather than extends. Desktop notes sit in the margin beside their paragraph, visible without clicking — the _Glossa Ordinaria_ model the project is named after                                                                                           | Loosely coupled to the design pass (#4): could ship standalone but shares typography and spacing surface with it. The 2026-08-16 naming rule stands — a gloss must never be confusable with its source, visually or structurally                                                                        | Estimate: medium                                                                                                                   |
| 4   | **Design pass**                                                 | `app.css` still declares its colours placeholders, and there are no ornaments — promised in `docs/decisions.md`'s original stack/design decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Independent of everything else here                                                                                                                                                                                                                                                                     | Not sized — deliberately open-ended (mockups were always meant to precede this, per the original decision)                         |
| 5   | **CCC `related` (marginal concordance) + `abbreviations.json`** | Both are absent from the vatican.va CCC mirrors, and both fields are ready and empty: `related` is a per-paragraph array in `paragraphs.json`, emitted `[]` on all 2,865 paragraphs in both editions (`ccc.py`, with the absence recorded in the manifest notes); `abbreviations.json` is 3 bytes, `{}`. **Be precise about what the sigla table would buy**, because it is less than it looks: it replaces the hand-built stopgap in `refs-grammar.ts` (`DOCUMENT_SIGLA_EN`/`_PT`, assembled by counting occurrences with `jq`, self-described as not exhaustive) and so improves the _expansion tooltip_ layer. It creates no new links — DS, CIC, CCEO, PL, PG, AAS, ND and the liturgical-book sigla have nothing in the corpus to point at either way. `related` is the half with reader-visible reach | A non-vatican.va source for `related` (candidates in `link-surface.md`: scborromeo.org, catholiccrossreference.online). `abbreviations.json` has two independent paths — each ingested document's own title (`link-surface.md` #4) or the Latin CCC's populated `abbrev_lt.htm` (`latin-sources.md` §5) | Not sized — depends on which source is chosen; the abbreviations half may be nearly free once either path lands                    |
| 6   | **Matos Soares footnotes**, **Compendium Appendix B formulas**  | Two items with different standing now. The Matos Soares notes are real but our chapter source prints only the markers; the sole structured source is vulgata.online — **which the Douay-Rheims ingestion has since scraped successfully**, so the "Cloudflare-blocked, availability unknown" premise no longer holds and this is ordinary scraping against a reachable source. Appendix B's doctrinal formula lists remain deliberately separate from the prayers work (`docs/corpus-schema.md`)                                                                                                                                                                                                                                                                                                            | Matos Soares: nothing but the work (note the notes are under the same copyright clock as the translation, PD 1 Jan 2028). Appendix B: a dedicated scope and parser for non-prayer formula lists                                                                                                         | Matos Soares — estimate: small-to-medium, a second pass over a source already parsed for another edition. Appendix B — not sized   |
| 7   | **The Fathers: a citation-parsing pass**                        | The Fathers are deliberately **not ingested** (2026-08-23, after measurement — `docs/research/summa-and-fathers.md`), and that is a decision, not a backlog item. What _is_ a backlog item is the recommendation made in its place: parse the citation strings already captured — **94% carry a work-internal locator** — buying reverse "cited in" panels and link-out targets for no fetches and no rights exposure                                                                                                                                                                                                                                                                                                                                                                                       | Nothing. Cheapest item on this list, clearest payoff                                                                                                                                                                                                                                                    | **Estimate: medium.** No fetches; the work is the abbreviation table and the two-language forms                                    |
| 8   | **CCC ¶ ↔ document section cross-references**                   | The one link surface in `docs/link-surface.md` (row #12, and `docs/corpus-schema.md` §Derived) still marked not built. `refs.ts` already links a siglum _forward_ when it names an ingested document; what is missing is the derived reverse index — "this section of _Lumen Gentium_ is cited by CCC ¶¶ …" — the same derivation `build-xrefs.mjs` performs twice already, both times against the Bible. **Forward resolution is in better shape than "sigla" suggests**: 16 sigla map to the Vatican II slugs (EN), and a title index built from `listDocuments()` resolves any of the 232 ingested documents cited by spelled-out title in either language — which is how the CCC cites encyclicals                                                                                                      | Not #5 — the title index already covers the documents a sigla table never would. The one real design point: `build-xrefs.mjs` never calls `setDocumentTitleSource`, so it parses no `documentTitle` segments at all today; a reverse index has to inject that source into the builder                   | Estimate: small. A third index in an existing builder, over strings already captured, using the grammar that renders the links     |
| 9   | **The Bible edition-divergence table**                          | `docs/research/bible-edition-divergence.md` proposes a reader-facing table of the four kinds of divergence and stops at "no formula relates them" for the sharpest cases. Adding the Clementine settled that: the Latin takes a side in **all 31** EN/PT disagreements (PT 25, EN 6, neither 0), so every entry can carry a documented base reading. The proposal is unbuilt (`docs/decisions.md`, 2026-08-23)                                                                                                                                                                                                                                                                                                                                                                                              | Nothing. The measurement is done                                                                                                                                                                                                                                                                        | Estimate: small — a documentation/table pass, not a pipeline change                                                                |
| 10  | **CIC, Denzinger, Roman Catechism, Vatican I**                  | Deliberately **out of scope**, restated for self-containedness: CIC has no Portuguese edition on vatican.va (hard language-symmetry blocker); Denzinger is Herder-copyrighted and never a vatican.va publication; the Roman Catechism and Vatican I were not found on vatican.va under any URL tried (Vatican I's absence isn't conclusively confirmed — no sitemap search attempted). Full detail: `docs/decisions.md` §Vatican documents in scope, `docs/research/vatican-documents.md` §2, §5                                                                                                                                                                                                                                                                                                            | —                                                                                                                                                                                                                                                                                                       | Out of scope, not sized                                                                                                            |

## Sequencing and recommendation

Nothing here gates anything else; order is a priority argument, not a dependency one. Two observations shape it.

- **#3 is the only row blocking data already paid for.** Every other gap here is work that adds something; sidenotes is work that _reveals_ something — an apparatus that cost a crawl and currently renders nowhere. That is a stronger claim on attention than the priority argument it used to compete on.
- **Nothing else here gates anything.** #8 was long assumed to wait on #5's sigla table; it does not. The CCC cites conciliar documents by siglum and encyclicals by spelled-out title, and both already resolve — the sigla that remain unresolvable name works that are not in the corpus at all, so no table makes them linkable.
- **#3 and #4 share typography and spacing surface**, so doing them together is more efficient than sequencing them — a scheduling convenience, not a dependency.

**Recommended order:**

1. **#3 sidenotes**, ideally alongside **#4 design pass** for the shared surface. This is where the Challoner apparatus becomes visible.
2. **#7 the Fathers citation pass**, **#8 the reverse document index**, **#9 the divergence table** — the cheap, no-fetch, already-measured tier. Each is small enough to slot between larger phases.
3. **#2 search** — the largest unscoped item; needs a prototype before it can be planned.
4. **#1 Latin for the CCC and documents** — scoped below.
5. **#5 `related` / #6 Appendix B** — lowest urgency; each still needs a research pass in `docs/research/` style before implementation is scopeable. (#6's Matos Soares half no longer does, and can move up with #2.)

## Latin — the CCC and the document families

`docs/research/latin-sources.md` establishes, with live 2026-08-15 fetches, that Latin is fetchable and addressable for all 16 Vatican II documents, the CCC, and a real if uneven slice of encyclicals — notably filling exactly the gap where Portuguese is weakest, because Latin was the actual promulgation language. **The Compendium has no Latin edition anywhere on vatican.va**: a permanent, source-level fact to design around, not a scrape target.

The architecture question this phase used to be blocked on — UI language vs. content language — was decided and built on 2026-08-23; `docs/decisions.md` has the reasoning and the alternatives considered. What remains:

- **The two-URL-template reality** (`_lt.html` suffix for the "archive" template families vs. a `/la/` path segment for the "content" families, `latin-sources.md` §4) needs a scraper design. This is the same split `vatican_docs.py` already routes for EN/PT, so it is a third `lang` argument rather than a new family — but nobody has written it.
- **`check_language_symmetry` hardcodes two languages at both ends.** `_WORK_ID_RE` accepts only `en`/`pt` as trailing language codes, so a Latin work directory would be silently skipped rather than checked, and the comparison is pairwise-for-exactly-two. Generalising the mechanism is mechanical.
- **What that check should _assert_ once Latin is in it is not mechanical, and is undecided.** It currently reads a section-count mismatch as evidence of a parsing defect, which is reasonable between two independent translations. Latin is the text the vernaculars translate, so a Latin/vernacular mismatch may be what translation _is_ rather than a bug. Comparing each vernacular **against Latin when Latin is present**, rather than comparing all present languages pairwise, is the plausible better check. The Bible offers a precedent worth reading first: there, adding the base text turned an unresolvable "both are faithful" into an adjudicable question (`docs/decisions.md`, 2026-08-23), which argues for the against-Latin shape.

  This is flagged here so it is not rediscovered mid-implementation — the way `check_language_symmetry` itself was originally motivated by a real, found defect (`docs/research/vatican-documents.md` §6).

- **The Latin CCC's populated `abbrev_lt.htm`** (`latin-sources.md` §5) is a second, independent path to #5's abbreviations gap — still not chosen, still not needed.
