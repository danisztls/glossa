# The corpus is never committed — copyrighted or scraped text stays out of the public repo.
corpus/

# Python
__pycache__/
.venv/

# Node / SvelteKit
node_modules/
site/.svelte-kit/
site/build/

# Empty dotfiles recreated by Claude Code's sandbox shell init — not project files
.bash_profile
.bashrc
.gitconfig
.gitmodules
.profile
.ripgreprc
.zprofile
.zshrc

# Editor / tooling state, not project files
.claude/
.idea/
.vscode/
.mcp.json

# Misc
.DS_Store
*.log
